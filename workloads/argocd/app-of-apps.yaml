apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: genai-app-of-apps
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: genai-platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/lehphyro/GenAIReferenceArchitecture.git
    targetRevision: main
    path: workloads/argocd/applications

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false

    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true

    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
