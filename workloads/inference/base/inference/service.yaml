apiVersion: v1
kind: Service
metadata:
  name: vllm-inference
  labels:
    app: vllm-inference
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: vllm-inference
